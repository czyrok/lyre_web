entryPoints:
  http:
    address: :80
    http:
      redirections:
        entryPoint:
          to: https
          scheme: https

  https:
    asDefault: true
    address: :443
    http:
      tls: true

# TODO: remettre en prod
# certificatesResolvers:
#   letsencrypt:
#     acme:
#       email: me@dylan.valentin.tech
#       storage: acme.json
#       httpChallenge:
#         entryPoint: https

providers:
  docker:
    endpoint: unix:///var/run/docker.sock
    exposedByDefault: false

accesslog:

# TODO: à retirer
# API and Dashboard configuration (optional)
api:
  dashboard: true
  insecure: true # Only for testing, secure it in production
